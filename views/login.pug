extends layouts/login-layout.pug

block content
    div.loginContainer
        h1 Login 
        form(action="", method="post")
            p.errorMessage #{errorMessage}
            input(type="text", name="logUsername", placeholder="Username or email", value='kazumyo' required='')  
            input(type="password", name="logPassword", placeholder="Password", required='' value='caio123')
            input(type="submit", value="Login")
        a(href="/register") Need an account? Register here.